# VisionXYZ Environment Variables
# Copy to .env.local and fill in values

# No server-side secrets required — all processing is client-side

# Optional: Analytics (privacy-preserving only)
# NEXT_PUBLIC_ANALYTICS_ID=

# Optional: Model CDN base URL (default: relative /models/)
# NEXT_PUBLIC_MODEL_BASE_URL=
